<?php
define('FCPATH', __DIR__ . DIRECTORY_SEPARATOR . 'public' . DIRECTORY_SEPARATOR);
require 'public/index.php'; // This might exit, wait, index.php runs the app.

// We can just use the CodeIgniter system directly or a spark command.

